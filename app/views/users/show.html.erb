<h1><%= @user.name %></h1>

<% @duties.each do |d| %>
    <%= link_to d.location.name, location_url(d.location_id), class: "btn btn-primary mt-1" %>
    <%= link_to "check duty details", location_duty_path(d.id), class: "btn btn-primary mt-1" %>
<% end %>